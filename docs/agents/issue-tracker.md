# Issue Tracker — Linear

This repo uses **Linear** for issue tracking.

## Read

- Skills query Linear via the Linear API
- Uses `linear-sdk` or Linear GraphQL API

## Write

- Creates issues in Linear via API
- Applies labels matching the canonical triage vocabulary

## Workflow

1. Query Linear for open issues
2. Create new issues with appropriate labels
3. Transition states using Linear API